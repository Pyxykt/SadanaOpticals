# SadanaOpticals

ClearGaze eyewear storefront (React + Vite) at the repo root.

## Scripts

```bash
npm install
npm run dev
```

- `npm run build` — production build (`dist/`)
- `npm run preview` — preview the production build

Routes: `/`, `/products`, `/product/:slug`, `/contact`
